<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Android 13+ (API 33+) gates NsdManager.discoverServices() behind
         either ACCESS_FINE_LOCATION or NEARBY_WIFI_DEVICES actually being
         GRANTED at runtime (a manifest declaration alone is not enough,
         same as any other dangerous permission since API 23) - without
         either one granted, discovery silently reports zero services
         instead of failing loudly. This app never uses nearby-device data
         for physical location, so neverForLocation avoids also implying a
         location-permission dependency for this one. See MainActivity's
         runtime permission request. -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- HYDRA-UMC-SERVER's server.ts (the backend this app talks to - a
         separate repo from HYDRA-UMC STUDIO's own web frontend since the two
         were split apart) is plain http/ws, no TLS, by design -
         REMOTE_API.md's own "trusted LAN, no auth" model. The
         target host is a dynamic user-entered IP (network/Discovery.kt),
         not a fixed domain, so a network-security-config domain allowlist
         isn't an option; usesCleartextTraffic is the only way this app can
         reach a real server at all on API 28+, which blocks cleartext by
         default. -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.App.Starting">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".widget.GlobalStopWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/global_stop_widget_info" />
        </receiver>
    </application>
</manifest>
